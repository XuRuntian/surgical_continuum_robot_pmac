/*For more information see notes.txt in the Documentation folder */
&1            // 激活 1 号坐标系
#1->A         // 将 1 号电机映射为 A 轴
#2->B         // 将 2 号电机映射为 B 轴
#3->C         // 将 3 号电机映射为 C 轴
#4->U         // 将 4 号电机映射为 U 轴
#5->V         // 将 5 号电机映射为 V 轴
open prog 1
// --------------------User Code Goes Here------------------------
Linear       // 开启线性插补模式 (多轴同启同停)
Abs          // 绝对位置模式 (配合你计算出的绝对脉冲)
Ta 100       // 加速时间 100 毫秒 (平滑防抖)
Ts 50        // S型曲线平滑时间 50 毫秒 (保护腱绳)
tm 500

// 执行多轴协同移动到 PLC 算好的 P 变量位置
A (target_pos1) B (target_pos2) C (target_pos3) U (target_pos4) V (target_pos5)
close

